# PolyAlpha Protocol — Technical Debt Report
> Generated by Claude Code | April 28, 2026 | Prompt 1 Audit

---

## Summary

| Category | Count |
|----------|-------|
| CRITICAL (P0 — blocks live trading) | 5 |
| MODERATE (P1 — degrades real UX) | 4 |
| ACCEPTABLE (P2 — cosmetic / expected for testnet) | 5 |
| **TOTAL** | **14** |

---

## Debt Table

| # | File & Line | Issue | Category | Priority | Fix |
|---|-------------|-------|----------|----------|-----|
| 1 | `agent/arb_scanner.py:38` | `MOCK_FUNDING_OPPORTUNITIES` used as fallback — OKX API returns HTTP 400; fake arbitrage signals silently returned as real | CRITICAL | P0 | Fix OKX endpoint URL to per-instrument query (`/api/v5/public/funding-rate?instId=BTC-USDT-SWAP`); add 3-retry backoff; tag result with `source: "mock"\|"real"` |
| 2 | `agent/arb_scanner.py:197,250` | `scan_funding_arbitrage()` returns `MOCK_FUNDING_OPPORTUNITIES[:3]` silently when all live APIs fail — no error flag, no logging | CRITICAL | P0 | Raise `RuntimeError` after exhausting retries, or return tagged `{"source": "mock", "error": "..."}` dict instead of bare list |
| 3 | `agent/arb_scanner.py:316` | `scan_polymarket_fast_resolution()` silently returns `MOCK_POLYMARKET_OPPORTUNITIES` on Gamma API failure — no caller knows it is mock data | CRITICAL | P0 | Return `{"data": [...], "source": "mock", "error": "Gamma API unavailable"}` so callers can gate on source |
| 4 | `agent/agent.py:128` | `[V2 STUB]` — `get_polymarket_v2_price()` is annotated as a stub; price may differ from real V2 CLOB; `execute_trade()` does not exist at all | CRITICAL | P0 | Implement `PolymarketExecutor` class with `py-clob-client-v2` SDK (Prompt 5 task) |
| 5 | `agent/pm_arb_agent.py:185` | Oracle risk score hardcoded to `0.1` for all markets — real `oracle_risk_score()` requires raw market dict not available in post-processed results | CRITICAL | P0 | Pass raw market dict from Gamma API through to `oracle_risk_score()`; do not use stub score as gating condition for live trades |
| 6 | `frontend/src/components/SocialFeed.js:2` | Entire social feed is hardcoded fake tweets ("Mock X (Twitter) market-alpha feed") — no real X/Twitter API integration | MODERATE | P1 | Replace with real Twitter/X API v2 bearer token search for `$PALPHA OR polymarket` (Prompt 5+) |
| 7 | `frontend/src/pages/PALPHAHubPage.js:152` | `MOCK_PROPOSALS` array hardcoded — DAO governance display does not read from `PolyAlphaDAO.sol` contract at `0x03C5...` | MODERATE | P1 | Call `ProposalCreated` events from contract via `ethers.js`; keep mock as `"cached data"` fallback (Prompt 5 Task 2) |
| 8 | `agent/arb_scanner.py:166` | Comment admits only 3 of 9 exchanges live (`HL`, `Binance`, `OKX`) — `Bybit/dYdX/Drift/Vertex/Paradex/Aevo` are stubs with no implementation | MODERATE | P1 | Implement remaining 6 exchange fetchers (Prompt 2 / funding-arb-scanner tools 4–6) |
| 9 | `agent/btc_signal.py:105` | `get_polymarket_v2_odds()` — TODO comment marks V2 SDK replacement not yet done; uses direct REST instead of SDK | MODERATE | P1 | Replace with `py-clob-client-v2` once SDK is integrated (Prompt 5) |
| 10 | `frontend/src/config.js:14` | `MOCK_USDC_ADDRESS` references testnet MockUSDC — expected during development | ACCEPTABLE | P2 | Replace with mainnet USDC address at mainnet launch |
| 11 | `scripts/deploy.js:24–30` | Deploys `MockUSDC` contract — intentional testnet scaffolding | ACCEPTABLE | P2 | Remove from production deploy script; use real USDC on mainnet |
| 12 | `frontend/src/pages/VaultPage.js:251,263` | HTML `placeholder` attributes on USDC amount inputs — cosmetic | ACCEPTABLE | P2 | No action needed |
| 13 | `frontend/src/pages/PALPHAHubPage.js:134,236,242,248` | HTML `placeholder` attributes on staking + governance form fields — cosmetic | ACCEPTABLE | P2 | No action needed |
| 14 | `agent/agent.py:431` | `datetime.utcnow()` deprecated since Python 3.12 — produces `DeprecationWarning` | ACCEPTABLE | P2 | Replace with `datetime.now(timezone.utc).isoformat()` in next cleanup pass |

---

## P0 Fix Order (This Sprint)

1. **OKX API fix** ← Prompt 1 Task 3 (fixing now)
2. **Source tagging on mock fallbacks** ← Prompt 1 Task 3 + Prompt 2 Task 1
3. **`execute_trade()` + Polymarket V2 SDK** ← Prompt 5
4. **`oracle_risk_score()` real data path** ← Prompt 2 (requires raw market dict piped through)

---

*Next: Prompt 2 (Dual-Track Data Pipeline) will resolve items #2, #3, and #8.*
