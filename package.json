{
  "name": "polyalpha-protocol",
  "version": "1.0.0",
  "description": "DAO-Governed AI Market-Making Vault on Polygon — ISOM3270 Final Project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "verify": "hardhat verify --network amoy",
    "node": "hardhat node",
    "push": "bash scripts/auto-push.sh",
    "push:msg": "bash scripts/auto-push.sh"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0"
  }
}
